import React from "react"
import { styled } from "@material-ui/core"

const Container = styled("div")({})

export const CreatePage = () => {
  return <Container>Create Page</Container>
}

export default CreatePage
